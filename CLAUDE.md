# alreadymorning.com — 작업 규칙

# 0. 🔴 우리는 **벌써아침**이다 — 다른 회사의 식별자를 절대 쓰지 마라

> **이 절은 이 파일에서 가장 먼저 읽는다.** 새 식별자(앱 ID·번들 ID·패키지명·도메인·이메일·저작권 표기·
> 스토어 등록 정보·git 설정)를 만들기 전에 반드시 여기를 확인한다. 대표 지시 2026-09-16.

이 레포의 소유자는 **벌써아침** (영문 `alreadymorning` / `Beolsseo Achim`, 대표 송현정, 개인사업자,
사업자등록번호 818-10-02994)이다. **이것 외의 어떤 회사도 우리가 아니다.**

## 허용되는 식별자 — 이 목록에 없으면 만들지 말고 대표에게 묻는다

| 종류 | 허용 값 |
|---|---|
| 회사명 | `벌써아침` · `alreadymorning` · `Beolsseo Achim` |
| 도메인 | `alreadymorning.com` (하위 도메인 포함) |
| 앱 ID · 패키지 · 번들 | `com.alreadymorning.*` (권장) 또는 **우리 제품명** 기반 — 던전 컴퍼니 Android `com.dungeoncompany.app` 처럼 |
| 이메일 | `*@alreadymorning.com` |
| 저작권 표기 | `© 벌써아침` / `© alreadymorning` |

(`beolsseo.com` 은 **블로그 전용**이며 회사와 연결해 표기하지 않는다 — 별도 규칙.)

## 🔴 절대 금지

- **우리 것이 아닌 어떤 회사의 이름·도메인·이메일·계정·조직 ID·약어도 — 어디에도.**
  앱 ID, 번들 ID, 패키지명, 네임스페이스, 저작권 표기, 스토어 등록, `git config`, 주석, 커밋 메시지, 문서, 테스트 픽스처까지.
- 🔴 **환경·도구가 자동으로 채운 조직명을 그대로 쓰지 마라.** 시스템 설정, git 전역 설정, IDE 프로필,
  자동완성, 스캐폴딩 도구의 기본값 — **이 경로로 남의 식별자가 새어 들어온다.** 그럴듯해 보여도 남의 것이다.
- 그 외 **모든 제3자 회사명**.

## 새 식별자를 만들 때

1. 위 허용 목록에서 고른다. **없으면 만들지 말고 대표에게 묻는다.**
2. 🔴 **스캐폴딩·셋업 도구가 자동으로 채운 값을 그대로 두지 마라.** `npm create` · `cap init` · IDE 템플릿이
   넣은 `appId`·`namespace`·`author`·`organization` 을 **전부 눈으로 확인**한다.
3. 만든 뒤 `grep -ri` 로 레포 전체에 우리 것이 아닌 조직명이 없는지 확인한다.

## 왜 이 규칙이 있나

**2026-09-15 던전 크러시 프로젝트 셋업에서 제3자 회사명이 앱 ID 로 들어갔다**(`dungeon-crush#19`).
`capacitor.config.ts` · `build.gradle` · `MainActivity.java` · `strings.xml` 4파일 6곳. 실기기에 그 패키지명으로 설치까지 됐다.
**스토어 등록 전에 잡았다.** 올라간 뒤였으면 앱 ID 는 **영구 확정**이라 되돌릴 수 없었다.
제3자 회사명을 우리 제품 식별자로 쓰는 것은 그 자체로 **상표·IP 문제**다.

---

## 1. (이 레포의 다른 규칙은 아직 없다 — 필요해지면 아래에 추가한다)
